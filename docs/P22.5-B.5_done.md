# P22.5-B.5 收口报告 — CoordinateSystem + 渲染集成 (影子模式 0 侵入)

**日期**: 2026-08-14
**状态**: ✅ 完成 (P22.5-B 整套收口)
**范围**: app.js 加 shadow block + exportScoreJson 接 Score Model + 10 新测试

---

## TL;DR

P22.5-B.5 落地 **P22.5-B 整套收口**:
- `runScoreModelShadow()` 新函数 — render 末尾跑 Score Model 完整 pipeline,失败只 console.warn
- `renderNotation` 末尾调 shadow block — 0 侵入
- `exportScoreJson` 接 scoreModel — 业务层复用 collectScoreDocument, 同时输出 Score Model JSON
- 10 新测试 / 3 class
- 全 pytest **630 passed** (baseline 620 + 10 B.5), 0 回归

**核心决策**: 影子模式 0 侵入 — app.js 渲染主循环不动,Score Model 作为 view 在 render 末尾同步跑一次验证。

---

## 5 决策 (用户全接受)

| # | 决策 | 落地 |
|---|------|------|
| 1 | B.5 范围 | (a) **影子验证 + documentData 接 Score Model** |
| 2 | 影子形式 | (a)+(d) **render 末尾 shadow + pytest 自动化** (双保险) |
| 3 | documentData 接入 | (a) **复用 collectScoreDocument**, 在 exportScoreJson 路径加 scoreModel 输出 |
| 4 | 关系生成在渲染 | (b) **渲染不动**, 仍看 entry 标记 (A.3 现状) |
| 5 | 测试方法 | (c) **pytest + node subprocess** (跟 B.1-B.4 一致) |

---

## 改动清单

| 文件 | 改动 | 大小 |
|------|------|------|
| `web/app.js` | `runScoreModelShadow()` 新函数 + `renderNotation` 末尾调 + `exportScoreJson` 加 scoreModel 输出 | 245492 → 247538 bytes (+2046) |
| `tests/test_p22_5_b_5_score_model.py` | **新文件** 10 测试 / 3 class / 11143 bytes |  |
| `web/score-model.js.bak.p22.5-B5` | (备份) B.4 后状态, 31288 bytes |  |

---

## 2 个改动点

### 3.1 `runScoreModelShadow()` 函数 (新)

```js
// P22.5-B.5 — Score Model 影子验证
// 跑 buildScore + buildRelations + validateRelations
// 不参与渲染, 失败只 console.warn
function runScoreModelShadow() {
  if (typeof window === "undefined" || !window.ScoreModel) return;
  const SM = window.ScoreModel;
  try {
    const isPiano = staffMode?.value === "piano";
    const legacy = isPiano
      ? {staffScores: {treble: {measures: staffScores?.treble?.measures || []}, bass: {measures: staffScores?.bass?.measures || []}}}
      : {scoreMeasures: staffScores?.[activeStaffKey()]?.measures || []};
    const score = SM.buildScore(legacy);
    SM.buildRelations(score);
    const v = SM.validateRelations(score);
    if (!v.valid) {
      console.warn("[P22.5-B.5 shadow] validateRelations errors:", v.errors, "warnings:", v.warnings);
    }
  } catch (e) {
    console.warn("[P22.5-B.5 shadow] pipeline failed:", e);
  }
}
```

### 3.2 `renderNotation` 末尾调 shadow (0 侵入, 1 行)

```js
  // 任何 VexFlow 异常都不能让 canvas 保持半渲染状态。
  try {
    if (isPianoMode()) {
      renderPianoNotation();
    } else {
      renderSingleNotationSafe();
    }
  } catch (error) {
    handleRenderError(error);
  }
  // P22.5-B.5 — 影子验证: render 末尾跑 Score Model 完整 pipeline
  runScoreModelShadow();
}
```

### 3.3 `exportScoreJson` 接 scoreModel 输出 (5 行)

```js
function exportScoreJson() {
  const documentData = collectScoreDocument();
  const validation = validateScoreDocument(documentData);
  // P22.5-B.5: 同时输出 Score Model JSON (供序列化测试 + 未来持久化)
  let scoreModelJson = null;
  try {
    if (typeof window !== "undefined" && window.ScoreModel) {
      // ... buildScore + buildRelations + JSON.parse(JSON.stringify(score))
    }
  } catch (e) {
    console.warn("[P22.5-B.5 exportScoreJson] Score Model build failed:", e);
  }
  scoreDataOutput.value = JSON.stringify({ score: documentData, scoreModel: scoreModelJson, validation }, null, 2);
}
```

---

## 测试覆盖 (10 测试, 3 class)

| Class | 数量 | 覆盖 |
|-------|------|------|
| `TestAppJsShadowStatic` | 4 | runScoreModelShadow 定义 / renderNotation 末尾调 / exportScoreJson 含 scoreModel / shadow fail-safe |
| `TestShadowPipeline` | 5 | 双谱表 / 单谱表 / 跟 legacy 数据一致 / 空 score 不抛 / 坏数据不抛 |
| `TestB1ToB5FullChain` | 1 | buildScore → relations → JSON → fromJSON → 重建 relations → JSON 完全一致 |

**关键测试**:
- `test_02_renderNotation_calls_shadow_at_end` — brace 计数定位 renderNotation 末尾,确保调 runScoreModelShadow();
- `test_03_legacy_data_consistency` — legacy 3 note → Score 3 note,chord 2 音 + rest kind 保留
- `TestB1ToB5FullChain::test_01` — B.1-B.5 完整 round-trip, JSON 字符串完全相等

**总测试统计**:
- B.1 score_model: 19 测试 ✅
- B.2 conversion: 15 测试 ✅
- B.3 cross_measure_relations: 21 测试 ✅
- B.4 json_serialization: 13 测试 ✅
- B.5 render_integration: 10 测试 ✅
- P22.5-B 合: **78 测试** ✅
- **全 pytest: 630 passed** (baseline 620 + 10 B.5)
- **0 回归** ✅

---

## HTTP 端到端

| 项 | 状态 |
|----|------|
| `node --check web/app.js` | exit 0 ✅ |
| `node --check web/score-model.js` | exit 0 ✅ (B.4 后未动) |
| `node -e` 实际跑 B.1-B.5 完整链路 | ✅ (`full chain: Score \| valid: true \| rel count: 2 \| json equal: true`) |
| 浏览器实测 (server PID 19632 跑老 server.py) | ⚠️ 需用户重启 server 才能浏览器实测 |

---

## B.5 解决了什么 / 没解决什么

### 解决
- ✅ app.js 真的接进 Score Model 数据流
- ✅ shadow block 0 侵入, 失败只 console.warn
- ✅ exportScoreJson 同时输出 legacy documentData + Score Model JSON
- ✅ B.1-B.5 完整链路 round-trip 数据完全一致
- ✅ 3 套模式全过: 空 / 单谱表 / 双谱表

### 没解决 (后续 P22.x)
- ❌ A.5/A.6 浏览器边角 (用户明确"不要碰")
- ❌ B.6 / P22.6 schema 包装 + 持久化 (留后续)
- ❌ MusicXML 接入 Score Model (B.4 决策 5 留独立阶段)
- ❌ 渲染主循环切到 Score Model (留 P22.7 / 未来)

---

## 已知 B.5 限制 (接受)

1. **shadow block 不接 cs.updateLayout** — CoordinateSystem 跟 Score Model 解耦, 各自独立
2. **exportScoreJson 输出双 JSON** — 同时含 documentData + scoreModel, 体积变大
3. **B.3 buildRelations 追加语义** — round-trip 时 fromJSON 还原的 relations 不会被覆盖, 测试需先清空
4. **shadow 不消费 score.relations** — 关系是序列化概念, 渲染循环仍看 entry 标记
5. **server 未重启** — 浏览器实测需用户手动重启

---

## 备份汇总

| 文件 | 大小 | 状态 |
|------|------|------|
| `web/score-model.js` | 31288 bytes | B.5 后 (B.5 不改 score-model.js) |
| `web/score-model.js.bak.p22.5-B5` | 31288 bytes | B.4 后 / B.5 实施前 |
| `web/app.js` | 247538 bytes | B.5 后 |
| `tests/test_p22_5_b_5_score_model.py` | 11143 bytes | **新** |
| `docs/P22.5-B.5_done.md` | (本文件) | B.5 收口 |

恢复命令 (B.5 整套回滚):
```bash
# app.js 改动手动 revert (git checkout)
cp web/score-model.js.bak.p22.5-B5 web/score-model.js
rm tests/test_p22_5_b_5_score_model.py
rm docs/P22.5-B.5_done.md
```

---

## P22.5-B 累计 (整套收口)

| 阶段 | 状态 | 改动量 |
|------|------|--------|
| **B.1 基础数据结构** | ✅ | 9165 bytes (6 class + factory) |
| **B.2 转换层** | ✅ | +7190 bytes (4 helper) |
| **B.3 跨 measure 关系 + Note 重命名** | ✅ | +8144 bytes (扩展 + 重命名) |
| **B.4 JSON 序列化** | ✅ | +6789 bytes (5 toJSON + scoreFromJSON) |
| **B.5 渲染集成 (影子模式)** | ✅ | app.js +2046 bytes (shadow block + exportScoreJson) |
| **整套** | ✅ | **score-model.js 31288 + app.js 247538** |

---

## 总结

**P22.5-B 整套 5 阶段全部完成** ✅
- **6 个 class** (Score / Part / Measure / Voice / Note / Relation) + 1 factory
- **4 个 helper** (buildLegacyFromScore / buildRelations / validateRelations / syncEntryFromRelation) + 1 scoreFromJSON
- **app.js 影子集成** — 0 侵入, render 末尾 shadow 验证
- **78 P22.5-B 测试 + 630 全 pytest + 0 回归**

P22.5-B 整套后, Score Model 已经有:
- 完整数据契约 (B.1)
- 双向转换 (B.2)
- 跨 measure 关系 (B.3)
- JSON 序列化 (B.4)
- 影子集成 (B.5)

下一步:
1. **回 P20/P21** (MTRE / Agent runtime)
2. **重启 server** (浏览器实测整套)
3. **MusicXML 接入 Score Model** (B.4 决策 5 独立阶段)
4. **P22.6 schema 包装 + 持久化** (未来)
5. 别的方向
